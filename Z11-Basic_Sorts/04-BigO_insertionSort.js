/**
 * Insertion sort is O(n) only if the array is almost sorted. Otherwise, it is O(n^2)
 */

/**
 * 
 * Note: All these sorting algorithms sort elements within the same array. They don't retuen a new array.
 * 
 */
/**
 * 
 * A binary tree has two nodes left and right.
 * 
 * Every node of full tree either points to two nodes or zero node.
 * 
 * Perfect tree means every line has items in it.
 * 
 * The top item is a parent in tree.
 * 
 * A node that doesn't have any children is called a leaf.
 * 
 * 
 */

/**
 * Big O of Binary tree
 * 
 * Find, Remove, Insert, take O(log n) because we use divide and conquer rule here
 */

/**
 * BST is always null terminated i.e. the last node points to null
 */